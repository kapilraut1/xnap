up:
	cd casplit-backend && docker-compose down && docker-compose up --build -d
	cd ..
	cd casplit-frontend && docker-compose down && docker-compose up --build -d
	cd ..

down:
	cd casplit-backend && docker-compose down
	cd ..
	cd casplit-frontend && docker-compose down
	cd ..

